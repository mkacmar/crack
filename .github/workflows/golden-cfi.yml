name: "Golden: CFI"

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/golden-build.yml
    with:
      rule: cfi
      # No compiler-rt CFI support for arm/riscv64 in Alpine
      arm: false
      riscv64: false
