name: "Golden: x86 CET IBT"

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/golden-build.yml
    with:
      rule: x86-cet-ibt
      arm64: false
      arm: false
      riscv64: false
